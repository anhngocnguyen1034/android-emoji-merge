<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- App chỉ có một tông tối nên dùng thẳng theme tối, không cần values-night. -->
    <style name="Theme.Emojimerge" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/window_ink</item>
    </style>
</resources>
